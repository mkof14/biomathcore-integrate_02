model Catalog {
  id          String   @id @default(cuid())
  slug        String   @unique
  title       String
  description String?
  priority    Int
  status      String   @default("ACTIVE")
  price       Int      @default(5)
}
