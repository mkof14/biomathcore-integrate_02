import Link from "next/link";
import Image from "next/image";

export default function Header() {
  return (
    <header className="sticky top-0 z-40 border-b border-white/10 bg-slate-900/80 backdrop-blur">
      <div className="pointer-events-none absolute inset-0 bg-gradient-to-r from-sky-500/20 via-transparent to-emerald-500/20" />
      <div className="relative mx-auto max-w-7xl px-4 py-3 flex items-center justify-between">
        <Link href="/" className="flex items-center gap-3">
          <Image
            src="/images/BMCore-Logo-33.png"
            alt="BioMath Core"
            width={36}
            height={36}
            className="h-9 w-9 rounded-xl"
            priority
          />
          <span className="text-xl font-semibold bg-gradient-to-r from-sky-400 to-white bg-clip-text text-transparent">
            BioMath Core
          </span>
        </Link>

        <nav className="hidden md:flex items-center gap-6 text-sm">
          <Link href="/services" className="text-slate-200 hover:text-white">Services</Link>
          <Link href="/pricing" className="text-slate-200 hover:text-white">Pricing</Link>
          <Link href="/catalog" className="text-slate-200 hover:text-white">Catalog</Link>
        </nav>

        <div className="flex items-center gap-4 text-sm">
          <Link href="/sign-in" className="text-slate-200 hover:text-white">Sign In</Link>
          <span className="text-slate-500">/</span>
          <Link href="/auth/sign-up" className="text-slate-200 hover:text-white">Sign Up</Link>
        </div>
      </div>
    </header>
  );
}
